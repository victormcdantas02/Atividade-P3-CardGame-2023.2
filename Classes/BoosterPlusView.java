public class BoosterPlusView extends BoosterView {
    public void exibirCartaUnica(Carta carta) {
        // Implemente a lógica para exibir a carta única
        System.out.println("Carta única gerada: " + carta.getNome() + " - " + carta.getTipo());
    }
}
